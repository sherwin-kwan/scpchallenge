extends layout

block content
  h1= title
  p Welcome to #{title}
  h1 Dynamic content

  if error
    p Error getting dynamic content, sorry!
  else
    p In the database, there are currently:
    ul
      li #[strong Players: ] !{data.player_count}
      li #[strong Teams: ] !{data.team_count}
      // !{data.player_count}



